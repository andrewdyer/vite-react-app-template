import { render } from '@testing-library/react';
import App from './App';

describe('App', () => {
  test('should render the App component without error', () => {
    render(<App />);
    // ...existing code...
  });
});
